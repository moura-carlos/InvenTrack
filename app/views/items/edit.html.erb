<%= render "shared/errors", object: @item %>

<%= simple_form_for(@item) do |f| %>
  <%= f.input :title %>
  <%= f.input :description %>
  <%= f.input :main_image, as: :file %>

  <%= f.submit %>
<% end %>

<%= link_to "Go back", @item %>
