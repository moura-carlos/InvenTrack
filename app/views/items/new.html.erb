<%= render "shared/errors", object: @item %>

<%= simple_form_for(@item) do |f| %>
  <%= f.input :title %>
  <%= f.input :description %>
  <%= f.submit %>
<% end %>

<%= link_to "All items", items_path %>
